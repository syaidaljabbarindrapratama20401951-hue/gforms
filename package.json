{
  "name": "google-form-tka-shell",
  "version": "2.4.0",
  "private": true,
  "description": "Tampilan ujian TKA/CBT untuk Google Forms",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check netlify/functions/fetch-form.js && node --check netlify/functions/submit-form.js"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
